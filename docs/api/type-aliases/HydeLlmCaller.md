# Type Alias: HydeLlmCaller()

> **HydeLlmCaller** = (`systemPrompt`, `userPrompt`) => `Promise`\<`string`\>

Defined in: [packages/agentos/src/rag/HydeRetriever.ts:115](https://github.com/framersai/agentos/blob/ac1e60f8857aef619a8160a2a7cfc7a63e5ee780/src/rag/HydeRetriever.ts#L115)

Minimal interface for the LLM call needed by HyDE.

## Parameters

### systemPrompt

`string`

### userPrompt

`string`

## Returns

`Promise`\<`string`\>

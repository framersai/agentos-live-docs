# Type Alias: Agency

> **Agency** = `Agent`

Defined in: [packages/agentos/src/api/types.ts:1129](https://github.com/framersai/agentos/blob/e72831f0f0d93a558f6ab38097d3d29cfcd4c629/src/api/types.ts#L1129)

An `Agency` is structurally identical to an `Agent` but is returned by
`agency()` and coordinates multiple underlying sub-agents.

# Type Alias: Agency

> **Agency** = `Agent`

Defined in: [packages/agentos/src/api/types.ts:1129](https://github.com/framersai/agentos/blob/7021709ae8e384df5464f1e2ae8b3fca40f72dbb/src/api/types.ts#L1129)

An `Agency` is structurally identical to an `Agent` but is returned by
`agency()` and coordinates multiple underlying sub-agents.

# Type Alias: Agency

> **Agency** = `Agent`

Defined in: [packages/agentos/src/api/types.ts:937](https://github.com/framersai/agentos/blob/ac1e60f8857aef619a8160a2a7cfc7a63e5ee780/src/api/types.ts#L937)

An `Agency` is structurally identical to an `Agent` but is returned by
`agency()` and coordinates multiple underlying sub-agents.

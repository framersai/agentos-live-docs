# Type Alias: Agency

> **Agency** = `Agent`

Defined in: [packages/agentos/src/api/types.ts:1129](https://github.com/framersai/agentos/blob/c3150c4c6250fd94284bfc6164282706975b97a8/src/api/types.ts#L1129)

An `Agency` is structurally identical to an `Agent` but is returned by
`agency()` and coordinates multiple underlying sub-agents.

# Function: parseSkillFrontmatter()

> **parseSkillFrontmatter**(`content`): `object`

Defined in: [packages/agentos/src/skills/SkillLoader.ts:32](https://github.com/framersai/agentos/blob/c3150c4c6250fd94284bfc6164282706975b97a8/src/skills/SkillLoader.ts#L32)

Parse YAML frontmatter from SKILL.md content.
Supports the standard `---` delimited format.

## Parameters

### content

`string`

## Returns

`object`

### body

> **body**: `string`

### frontmatter

> **frontmatter**: [`ParsedSkillFrontmatter`](../type-aliases/ParsedSkillFrontmatter.md)

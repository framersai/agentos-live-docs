# Function: parseSkillFrontmatter()

> **parseSkillFrontmatter**(`content`): `object`

Defined in: [packages/agentos/src/cognition/skills/SkillLoader.ts:32](https://github.com/framersai/agentos/blob/63ed327fe991cbf5fe1e01bca76416a3aaa76167/src/cognition/skills/SkillLoader.ts#L32)

Parse YAML frontmatter from SKILL.md content.
Supports the standard `---` delimited format.

## Parameters

### content

`string`

## Returns

`object`

### body

> **body**: `string`

### frontmatter

> **frontmatter**: [`ParsedSkillFrontmatter`](../type-aliases/ParsedSkillFrontmatter.md)

# Function: parseSkillFrontmatter()

> **parseSkillFrontmatter**(`content`): `object`

Defined in: [packages/agentos/src/skills/SkillLoader.ts:32](https://github.com/framersai/agentos/blob/e72831f0f0d93a558f6ab38097d3d29cfcd4c629/src/skills/SkillLoader.ts#L32)

Parse YAML frontmatter from SKILL.md content.
Supports the standard `---` delimited format.

## Parameters

### content

`string`

## Returns

`object`

### body

> **body**: `string`

### frontmatter

> **frontmatter**: [`ParsedSkillFrontmatter`](../type-aliases/ParsedSkillFrontmatter.md)

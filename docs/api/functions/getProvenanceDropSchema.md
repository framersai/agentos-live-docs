# Function: getProvenanceDropSchema()

> **getProvenanceDropSchema**(`prefix?`): `string`

Defined in: [packages/agentos/src/provenance/schema/provenance-schema.ts:97](https://github.com/framersai/agentos/blob/e72831f0f0d93a558f6ab38097d3d29cfcd4c629/src/provenance/schema/provenance-schema.ts#L97)

Generate SQL to drop all provenance tables (for testing/cleanup).

## Parameters

### prefix?

`string` = `''`

## Returns

`string`

# Function: getProvenanceDropSchema()

> **getProvenanceDropSchema**(`prefix?`): `string`

Defined in: [packages/agentos/src/safety/provenance/schema/provenance-schema.ts:97](https://github.com/framersai/agentos/blob/63ed327fe991cbf5fe1e01bca76416a3aaa76167/src/safety/provenance/schema/provenance-schema.ts#L97)

Generate SQL to drop all provenance tables (for testing/cleanup).

## Parameters

### prefix?

`string` = `''`

## Returns

`string`

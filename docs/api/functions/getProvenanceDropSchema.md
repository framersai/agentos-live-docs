# Function: getProvenanceDropSchema()

> **getProvenanceDropSchema**(`prefix?`): `string`

Defined in: [packages/agentos/src/provenance/schema/provenance-schema.ts:97](https://github.com/framersai/agentos/blob/369f4181e3a31735ff56401807893a6801760447/src/provenance/schema/provenance-schema.ts#L97)

Generate SQL to drop all provenance tables (for testing/cleanup).

## Parameters

### prefix?

`string` = `''`

## Returns

`string`

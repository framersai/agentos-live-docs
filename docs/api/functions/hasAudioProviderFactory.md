# Function: hasAudioProviderFactory()

> **hasAudioProviderFactory**(`providerId`): `boolean`

Defined in: [packages/agentos/src/media/audio/index.ts:100](https://github.com/framersai/agentos/blob/c3150c4c6250fd94284bfc6164282706975b97a8/src/media/audio/index.ts#L100)

Check whether an audio provider factory is registered for the given ID.

## Parameters

### providerId

`string`

Provider identifier to check.

## Returns

`boolean`

`true` if a factory exists for this provider.

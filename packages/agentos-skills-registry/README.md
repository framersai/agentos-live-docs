# @framers/agentos-skills-registry

Curated skills registry bundle for AgentOS.

This package provides helpers for:

- Discovering available curated skills from `@framers/agentos-skills`
- Loading bundled skills into a `SkillRegistry`
- Building prompt snapshots for injection into an agent system prompt
